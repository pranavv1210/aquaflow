enum PaymentStatus { unpaid, partial, paid }

enum DeliveryStatus { orderReceived, assigned, onTheWay, delivered }

enum VehicleStatus { available, busy, inactive }

enum DriverStatus { available, busy, inactive }

enum VehicleType { tractor, canter, partner }

enum ExpenseType { diesel, driverPayment, service, repair, police, tyre, other }
